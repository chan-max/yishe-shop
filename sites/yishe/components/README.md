# Yishe Components

Place Yishe-specific visual components here. They are auto-registered with the
`Site` prefix, so `Hero.vue` is available as `<SiteHero />`.

Components in this directory may change layout and interaction, but should call
shared composables for product, account, favorite and SEO behavior.
